language: python
python:
  - '3.8.5'

before_script:
  - pip install -r requirements.txt

env: DJANGO_SETTINGS_MODULE="elearning.settings.travis"

services:
  - postgresql

env: DJANGO_SETTINGS_MODULE="elearning.settings.travis"

script:
  - ./manage.py test courses.tests.test_models.SubjectModelTest
  - ./manage.py test courses.tests.test_models.CourseModelTest
  - ./manage.py test courses.tests.test_models.ModuleModelTest

branches:
  only:
    - dev